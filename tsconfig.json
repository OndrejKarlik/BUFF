// WARNING: this is used *only* for eslint running inside Visual Studio IDE and for web projects.
// It is *not* used for web UI inside C++ projects
// It more or less copies the per-file configs generated by cmake that we use in C++ projects

{
    "compilerOptions": {
        "target": "esnext",
        "strict": true,
        "baseUrl": ".",
        "paths": {
            "LibUltralight/*": ["Buff/LibUltralight/assets/*"]
        }
    }
}
