{
    "name": "ts-dev-server",
    "version": "0.0.0",
    "description": "TsDevServer",
    "main": "src/TsDevServer.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc --build .",
        "clean": "tsc --build --clean"
    },
    "devDependencies": {},
    "dependencies": {}
}
